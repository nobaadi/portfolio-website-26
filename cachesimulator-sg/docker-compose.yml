services:
  backend:
    build: ./backend
    ports:
      - "8001:8001"
    volumes:
      - sqlite_data:/data
    environment:
      DATABASE_PATH: /data/cache_sim.db

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend

volumes:
  sqlite_data:
